# OfficeWhere Unreleased

Current `main` contains changes after the `v0.7.13` tag. Move or rewrite these notes when preparing the next `vX.Y.Z` release note.

## Added / changed

- Nothing yet.

## Verification notes

- Rerun the release checklist on the final tag commit.
